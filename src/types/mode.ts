/**
 * Top-level app mode. Toggled top-right; auto-switches to 'race' when the
 * race timer arms. Cruise-mode feature set is light in v1, full in post-v1.
 */
export type AppMode = 'race' | 'cruise';
